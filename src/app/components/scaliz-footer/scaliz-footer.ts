import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-scaliz-footer',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './scaliz-footer.html',
  styleUrl: './scaliz-footer.scss'
})
export class ScalizFooterComponent {}
